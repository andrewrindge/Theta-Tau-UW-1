import { Stack, Text } from "@chakra-ui/react";
import { BannerArticleData, ButtonData, ContentSliderData, ContentSliderResponse, Test } from "../../lib/types";

interface Props {
    props: ContentSliderResponse
}

export default function ContentSlider({ props }: Props) {

    return (
        <Stack>
            { }
        </Stack>
    )
}






// const next = () => {
//     if (counter + 1 < res.) setCounter(counter + 1)
//     else setCounter(0)
// }

// const previous = () => {
//     if (counter - 1 >= 0)
// }